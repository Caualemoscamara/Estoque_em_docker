services:
  server:
    build: .
    container_name: server
    command: ./server
    ports:
      - "8080:8080"
    networks:
      - appnet

  client:
    build: .
    container_name: client
    command: ./clientDocker server 8080
    stdin_open: true
    tty: true
    depends_on:
      - server
    networks:
      - appnet

networks:
  appnet: